<!-- Sidebar -->
<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ path('app_dashboard') }}">
        <div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-laugh-wink"></i>
        </div>
        <div class="sidebar-brand-text mx-3"> {{ app.user.firstName }}</div>
    </a>

    <!-- Divider -->
    <hr class="sidebar-divider my-0">

    <!-- Nav Item - Dashboard -->
    <li class="nav-item">

        <a class="nav-link" href="{{ path('app_dashboard') }}">
            <h6 class="{{ app.request.requesturi == '/dashboard' ? 'font-weight-bolder' : '' }}">Dashboard</h6></a>
        {% if(app.user.roles[0] == 'ROLE_ADMIN') %}
            <a class="nav-link" href="{{ path('app_user') }}">
                <h6 class="{{ app.request.requesturi == '/user' ? 'font-weight-bolder' : '' }}">User</h6></a>
        {% endif %}
        <a class="nav-link" href="index.html">
            <h6 class="{{ app.request.requesturi == '/event' ? 'font-weight-bolder' : '' }}">Event</h6></a>
        <a class="nav-link" href="index.html">
            <h6 class="{{ app.request.requesturi == '/categories' ? 'font-weight-bolder' : '' }}">Categories</h6>
        </a>
        <a class="nav-link" href="index.html">
            <h6 class="{{ app.request.requesturi == '/sponsors' ? 'font-weight-bolder' : '' }}">Sponsors</h6></a>
    </li>
</ul>
<!-- End of Sidebar -->